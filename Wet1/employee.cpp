#include "employee.h"

Employee::Employee(int employee_id, int employee_salary, int employee_grade, int employees_company_id):
                    employee_id(employee_id),
                    employee_salary(employee_salary),
                    employee_grade(employee_grade),
                    employees_company_id(employees_company_id){};